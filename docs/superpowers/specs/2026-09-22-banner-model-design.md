# 배너 모델 재설계

작성 2026-09-22. 상태: 설계, 승인 대기.

## 배경

배너(웹 상단에 뜨는 당일 행사 카드)는 2026-08에 사람이 문장을 직접 적는 구조로
시작했다. `amount`, `period`, `extra` 세 칸에 화면에 나갈 글자를 그대로 적었다.

2026-09-18에 구조 필드(`BannerSpec`)를 넣었다. 수집기와 웹이 `extra` 문장을 되짚어
브랜드와 금액을 파싱하던 자리가 하루에 세 번 틀렸기 때문이다. 이때 "문장 칸이
적혀 있으면 문장이 이긴다"를 이행 규칙으로 두었다. 그 이행이 아직 안 끝났다.

## 현재 상황

한 배너의 한 값이 최대 세 층에 산다. 문장 칸, 구조 칸(`BannerSpec`), 최상위 칸이다.

## 문제

여덟 가지를 확인했다.

1. **최소주문금액이 세 군데다.** 최상위 `minOrder`, `items[].minOrder`, 그리고
   `extra` 문장 안의 "16,000원↑".
2. **브랜드가 네 군데다.** 최상위 `brand`, `items[].brand`, 파생값 `brands`,
   그리고 2026-09-21에 추가한 `brandLabels`.
3. **`period`가 두 가지를 겸한다.** 기간(2026-09-22부터 2026-09-30까지)과
   한정(선착순, 소진 시 종료)이다. 한정은 `limit`, `usage`라는 구조 칸으로도
   따로 있어서, 같은 사실을 산문과 열거값 두 벌로 적는다.
4. **`opensAt`이 세 겹이다.** 배너 수준, `items[]` 수준, 그리고 `period` 문장.
5. **`platform`이 감시값을 겸한다.** `own`은 플랫폼 이름이 아니라 "배달 플랫폼이
   아님"이라는 뜻이고, 동시에 오퍼 비교에서 빼라는 스위치다. 배달앱 4사 밖으로
   확장하면 이 칸이 먼저 깨진다.
6. **`amount`에 형태가 셋이다.** 문자열, `items[].amount` 숫자 목록, `amountRange`.
   퍼센트("최대 30%")와 적립은 구조 칸에 자리가 없어 문장으로만 존재한다.
   2026-09-22에 적립을 `limit: cashback`으로 적었는데, `limit`은 선착순이냐
   랜덤이냐를 말하는 축이라 적립이 들어갈 자리가 아니다.
7. **묶음을 되돌릴 수 없다.** 묶음 배너는 `items[]`를 가진 배너 한 건이다. 풀려면
   배너를 다시 쓴다. `url`이 최상위 한 칸이라 브랜드마다 링크가 다른 묶음은
   표현 자체가 안 된다.
8. **오퍼 조건 줄과 `extra`가 같은 값을 두 벌 그린다.** 배너가 오퍼로 설 때
   최소주문금액과 멤버십이 `extra` 문장에도, 상세의 조건 줄에도 뜬다.

## 선택

**배너 한 건은 브랜드 하나, 플랫폼 하나, 오퍼 하나다.** `items[]`를 없앤다.

**묶음은 명시한다.** `group` 칸에 같은 값을 적은 배너들이 한 장으로 그려진다.
링크나 기간이 같다고 자동으로 묶지 않는다. 조건이 같아도 묶을지 나눌지는
개발자가 정한다(사용자 결정 2026-09-22). 푸는 일은 한 배너의 `group`을 비우는
일이고, 배너를 다시 쓰지 않는다.

자동 추론(같은 링크·기간·플랫폼이면 묶기)을 버린 이유는 오묶음이다. 같은 날
같은 앱에서 성격이 다른 행사 둘이 우연히 같은 조건을 갖는 일이 실제로 있다.

## 새 구조

```yaml
- id: dunkin-20260922
  group: ddangyo-doubleday-0922    # 선택. 같은 값끼리 한 장
  brand: 던킨
  platform: ddangyo                # 열린 집합. 4사 밖도 값 하나 더
  comparable: true                 # 기본 true. own 성격이면 false
  url: "https://fdofd.ddangyo.com/gateway4.html?11sOkss"
  amount: {won: 7000}
  minOrder: 15000
  startsOn: 2026-09-22
  endsOn: 2026-09-22
  limit: none                      # none | first_come | random | targeted
  until: end_date                  # end_date | sold_out
  event: 더블 땡데이
```

바뀌는 칸만 적는다.

**`amount`가 한 가지 모양이다.** `{won:}` 또는 `{rate:}` 하나에 `kind`가 붙는다.
```yaml
amount: {won: 8000}                      # 8,000원
amount: {won: [null, 8000]}              # 최대 8,000원 (랜덤·상한)
amount: {won: [3000, 8000]}              # 3,000~8,000원
amount: {rate: 50, kind: cashback}       # 50% 적립
```
`kind`는 `discount`(기본), `cashback`, `point`다. 적립이 `limit`에서 나온다.

**`platform`에서 스위치를 뗀다.** `platform`은 이름만 갖는 열린 집합이다.
오퍼 비교에 들어갈지는 `comparable`이 말한다. 지금의 `own`은
`comparable: false`로 옮기고, 브랜드 자체 채널이라는 뜻은 `platform: own`이
그대로 갖는다.

**기간을 쪼갠다.** 문장 `period`가 사라지고 네 칸이 남는다.
`startsOn`/`endsOn`(날짜), `opensAt`("HH:MM" 매일 여는 시각),
`limit`(한정의 종류), `until`(무엇이 먼저 끝내는가: 날짜냐 소진이냐).
"~까지 선착순"은 `limit: first_come` + `until: end_date`이고,
"소진 시 종료"는 `until: sold_out`이다.

**브랜드는 이름 하나만 갖는다.** 정식명, 축약, 영문 구분은 `brands.yml`의
alias가 이미 갖고 있고 `BrandCatalog`가 푼다. 배너는 라벨을 저장하지 않는다.
파생 칸 `brands`, `brandLabels`는 묶음을 그릴 때 서버가 만든다.

**문장 칸을 없앤다.** `amount`, `period`, `extra`를 문자열로 적는 길을 막는다.
`BannerText`가 유일한 생산자가 된다. 2026-09-18에 시작한 이행을 여기서 끝낸다.

**`extra`가 조건과 기간을 흡수한다.** 최소주문금액, 멤버십, 채널, 한정, 기간이
한 문장으로 모인다. 배너에서 나온 오퍼는 상세의 조건 줄을 비우고 `extra`만
읽는다. 수집으로 들어온 일반 오퍼의 조건 줄은 그대로 둔다.

## 실행

1. 이 문서와 ADR을 올린다.
2. API에 새 모양을 넣는다. `BannerSpec`을 갈고 `BannerText`를 다시 쓴다.
   읽기는 한동안 두 모양을 다 받는다. 테스트가 두 모양을 다 덮는다.
3. 서버의 `banners.yml`을 옮기는 변환기를 쓴다. `--dry-run`으로 먼저 보고,
   ops로 적용하고, 적용 전 파일을 백업한다.
4. ops 콘솔은 `GET /ops/schema`를 읽어 그리므로 스키마만 바꾸면 따라온다.
   묶기/풀기 단추가 `group` 칸을 쓰게 고친다.
5. 웹이 `group`으로 묶어 그린다. `items[]` 경로를 지운다.
6. 옛 모양 읽기를 지운다.

2단계까지가 되돌릴 수 있는 지점이다. 3단계부터는 서버 파일을 바꾸므로
백업과 `--dry-run` 확인을 거친다.

## 다음 단계

승인을 받으면 writing-plans로 단계별 구현 계획을 만든다.

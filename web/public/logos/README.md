# 브랜드 로고 위치

파일명 = 브랜드명(원장에 찍힌 그대로, 대표명 별칭 아님) + `.png`.

예: `도미노피자.png`, `피자헛.png`, `빽보이피자.png`

- 정사각형 권장 (표시 크기 32×32px, `object-fit: cover`로 채움)
- 없으면 브랜드명 첫 글자로 자동 대체되므로, 없는 브랜드는 그냥 비워둬도 화면이 깨지지 않는다.
- 코드에서 파일명은 `encodeURIComponent(브랜드명)`으로 조회한다 (`App.jsx`의 `assetSrc`).

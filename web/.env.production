# 화면 갈래는 variant.js가 visitorId 해시로 정한다(반반, 재방문 고정).
#
# 여기에 VITE_UI_VARIANT=a 또는 b를 두면 그 값으로 못 박힌다 — 한쪽에서
# 사고가 나서 급히 전부 다른 쪽으로 돌려야 할 때 쓴다. 평소에는 비워둔다.
#
# 개별 확인은 ?variant=a / ?variant=b 로 한다. 배포를 건드리지 않는다.

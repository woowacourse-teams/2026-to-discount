# 지금 프로덕션이 낼 화면을 못 박는다.
#
# 배정은 variant.js가 visitorId 해시로 하지만, A안 화면이 아직 이 빌드에
# 들어오지 않았다. 고정하지 않으면 절반이 B를 보면서 이벤트에는
# variant: a로 찍혀 데이터가 통째로 못 쓰게 된다.
#
# 두 안이 다 들어오면 이 줄을 지운다 — 그때부터 배정이 산다.
VITE_UI_VARIANT=b

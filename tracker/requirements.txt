# tracker가 쓰는 표준 라이브러리 밖 의존성. 지금까지 어디에도 안 적혀
# 있었고, 그래서 CI에서 tracker 테스트가 한 번도 돌지 않았다(2026-08-23에
# Weekly Check를 붙이며 드러났다 — 첫 실행이 ModuleNotFoundError: yaml).
#
# 하한만 적는다. 상한을 박으면 보안 패치까지 막히는데, 이 셋 다 우리가
# 쓰는 범위(YAML 로드, 이미지 열기·자르기)에서 API가 흔들리지 않는다.
PyYAML>=6.0
Pillow>=10.0
